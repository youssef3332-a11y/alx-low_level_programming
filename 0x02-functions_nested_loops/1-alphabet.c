#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
	int i;

	i = (int)'a';
	for (i; i <= (97 + 26); i++)
	{
		putchar('i');
	}
	putchar('\n');
}
int main(void)
{
	print_alphabet();
	return (0);
}
